apiVersion: v1
kind: Service
metadata:
  name: iptables-service
  labels:
    run: iptables
spec:
  type: NodePort
  selector:
    run: iptables
  ports:
    - protocol: TCP
      port: 80
      name: http
      targetPort: 80
